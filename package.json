{
  "name": "ghostwire",
  "version": "1.2.0",
  "description": "GhostWire - Mullvad-inspired, self-hosted VPN panel with accounts, a modern web UI and native companion apps. WireGuard userspace; runs on Android (Termux), Linux, Windows, macOS.",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "ghostwire": "bin/ghostwire.js"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "vpn",
    "wireguard",
    "wireproxy",
    "mullvad",
    "anonymity",
    "privacy",
    "self-hosted",
    "termux",
    "android",
    "linux",
    "windows",
    "macos",
    "tunnel",
    "proxy",
    "socks5"
  ],
  "license": "MIT",
  "scripts": {
    "start": "node bin/ghostwire.js start",
    "dev": "node --watch bin/ghostwire.js start",
    "test": "node --test",
    "check": "node --check bin/ghostwire.js && node --check src/engine.js && node --check src/server.js && node --check src/qr.js && node --check src/util.js && node --check scripts/release.js",
    "release": "node scripts/release.js"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/node": "^24"
  }
}
