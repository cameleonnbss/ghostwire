<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The GhostWire panel is plain HTTP on the LAN / localhost. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
